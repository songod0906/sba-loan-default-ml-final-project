# Notebook

`final_workflow.ipynb` is the final review notebook for the project. It contains the cleaned workflow used for the submitted model comparison and reporting.

The raw dataset is not committed. To rerun, place `SBAnational.csv` in the project root and use the same local or Colab environment described in the notebook.
